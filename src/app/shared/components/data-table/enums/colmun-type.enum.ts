export enum TableColumnType {
    TEXT = "text",
    NUMBER = "number",
    DATE = "date",
    BOOLEAN = "boolean",
    ACTION = "action",
    STATUS = "status",
    SELECT = "select",
    ID = "id",
    USER = "user",
    CURRENCY = "currency",
}
